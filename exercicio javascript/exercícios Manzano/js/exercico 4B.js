alert("só positivo");
let numero = parseInt(prompt("digite um numero"));
let positivo;
if (numero < 0){
    positivar = numero * -1;
    alert ("o " + numero + "que era negativo, agora é positivo" + positivar)
}
else{
    alert ("o " + numero + "o numero já é positivo")
}
